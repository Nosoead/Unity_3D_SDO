using UnityEngine;

//어택 구현하려다 시간이없어서 못 했습니다.
public class PlayerAttack : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
